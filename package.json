{
  "name": "@kotoba-lang/macos-inventory",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only inventory of macOS persistence, privacy grants, extensions, signatures and installed software.",
  "type": "module",
  "scripts": {
    "test": "nbb --classpath src:test -m macos-inventory.test-runner",
    "dump": "nbb --classpath src -m macos-inventory.cli"
  }
}
